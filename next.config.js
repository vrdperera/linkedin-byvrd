/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['content.linkedin.com', 'rb.gy', 'images.unsplash.com'],
  },
};

module.exports = nextConfig;
